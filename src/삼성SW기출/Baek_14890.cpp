#include <bits/stdc++.h>

using namespace std;

int N, L;
int map[100][100];
int main(){
    scanf("%d %d",&N,&L);
    for (int i=0; i<N; i++)
    {
        for(int j=0; j<N; j++)
        {
            scanf("%d",map[i][j]);
        }
    }

}